//Write a program to solve the following quadratic equation which is having 3 input variables a, b, c. Assign float values
//of your choice into a and b variables and take the integer input value at run time in c variables. You have to evaluate the
// formula two times with – and + values so you have two output values must be saved in x1, and x2 variable.

//[Note:] Use the precedence rules for the arithmetic operators’ usage. For square root use built in function sqrt(x)
// which is available in math.h.

#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    int a = 3;
    int b = 5;
    int c, x1, x2;
    ((pow (b,3)- sqrt((b * b) - (3*a*pow(c,2)))));













    return 0;
}
